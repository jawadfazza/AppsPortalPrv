@using AppsPortal.Extensions;
@model DateTime?
@Html.TextBox("", Model.HasValue ? Localization.NumberFormat(Model.Value.ToString(Localization.Date())) : "", new { @class = "form-control date-picker " + Languages.CurrentCulture(),  autocomplete = "off"})
<span class="fa fa-calendar datebox"></span>
